import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <h1>My Web Application</h1>
            {...Header...}
        </header>
    );
};

export default Header;
